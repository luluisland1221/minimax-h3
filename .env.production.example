# Required public production origin. Do not use localhost in production.
NEXT_PUBLIC_BASE_URL=https://minimaxh3.pro

# Add Stripe, MiniMax, database, authentication, mail, and R2 values in
# Cloudflare's encrypted environment settings. Never commit secrets.
